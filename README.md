# Users example application

# Building application
Use provided wrapper by executing: `./mvnw clean package`

# Running application
`java -jar target/spring-rest-*-SNAPSHOT.jar`

or
 
 `sh start.sh`
